import React from 'react';
import LoginButton from "../components/LoginButton";

const Login = () => {
    return (
        <div>
            <h1>Log in page</h1>
            <LoginButton/>
        </div>
    );
};

export default Login;